---
status: accepted
---

# 聊天室附件採結構化引用

聊天室 AI 回覆以自然文字為主，另以結構化 `attachment_refs` 記錄實際使用的附件；前端將其呈現為可追溯的附件引用 chip，不要求模型在句子中硬寫 `[附件一]`。沒有 `#` 時不得產生附件引用，使用附件具體事實時應附上對應引用。

點擊引用 chip 對 `.txt/.md` 開啟現有閱讀器；若附件已刪除或不可用，僅顯示不可用狀態，不中斷整段聊天，也不在聊天氣泡中展開全文。
