[project]
name = "ai-council-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115.0", "pyyaml>=6.0", "uvicorn>=0.30.0"]

[project.optional-dependencies]
dev = ["httpx2>=0.28", "pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
