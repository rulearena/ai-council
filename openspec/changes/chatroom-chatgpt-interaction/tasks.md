## 1. Chatroom routing and `@` contract

- [x] 1.1 Add failing backend tests for plain-text Host routing, Host display-name chip, chip-only activation, hand-typed invalid role-like tokens, stale chip payloads, `all` precedence, duplicate chips, and no AI invocation before validation.
- [x] 1.2 Add the fixed `host` role to the chatroom mode definition, then add catalog and participant-projection tests for new and legacy meetings. Validated Persona fields and public summaries are intentionally deferred to 2.4.
- [x] 1.3 Implement the exact request schema: `content`, `mentions[{token_id,role_id,display_text,start,end}]`, `source_tokens[{token_id,source_ref,display_text,start,end}]`, ordered/deduplicated `source_refs`, and `quoted_event_id`; NFC-normalize before offset generation, reject non-NFC content, validate Python/Unicode code-point half-open spans, unique IDs, exact content slices, prefixes, bounds, overlap, and source_refs equality before routing. Use the complete table-driven 400 mapping (`INVALID_REQUEST_SCHEMA`, `MENTION_TOKEN_MISMATCH`, `STALE_MENTION_PAYLOAD`, `INVALID_MENTION_TOKEN`, `SOURCE_TOKEN_MISMATCH`, `STALE_SOURCE_PAYLOAD`, `INVALID_SOURCE_REF`, `SOURCE_NOT_VISIBLE_TO_TARGET`, `SOURCE_NOT_READABLE`) plus exact 404/409 envelopes.
- [x] 1.4 Make the backend token validator authoritative: NFC-normalize composer content before spans, scan uncovered raw @ candidates using the Unicode XID_Continue/email grammar, skip verified chip spans including spaces, apply normal `INVALID_MENTION_TOKEN` vs all-chip warning precedence, treat uncovered # as ordinary text, preserve original content plus token metadata in the human event, remove verified chip spans from normalized prompt instruction, and route every send through chatroom path.
- [x] 1.5 Update the runner and pending-role capture so Host is a normal target, multiple explicit roles fan out once each, `@all` includes Host, and mention-only instructions can reach the model as empty user text.
- [x] 1.6 Add frontend display-name role-chip autocomplete for Host, active roles, and all; chips retain hidden stable IDs and serialize structured mentions, never `@Advisor`/`@host` visible text; retain invalid drafts and surface exact ignored-invalid warnings.
- [x] 1.7 Add backend/frontend integration coverage for routing, event persistence, pending states, and the existing arrival-order `@all` display before moving to prompt changes.
- [x] 1.8 Add table-driven direct API/UI tests asserting full response bodies, field mapping, and zero event/job side effects for missing/wrong/unknown fields, blank/non-NFC content, malformed quote, well-typed missing quote graceful ignore, malformed/non-overlap/mismatched spans, stale mention/source payloads, every closed-union 400 code, exact 404/409 envelopes, CJK, NFC combining/non-NFC, punctuation boundaries, email, hand-typed same-label beside a real chip, duplicate chips, raw @all plus all-chip warning, and # chip plus hashtag.

## 2. Prompt layering and Persona

- [x] 2.1 Add failing adapter/runner audit tests for exactly ordered canonical chatroom messages `[system, developer, user]`, the labeled flattened prompt, byte-identical pre-transport `prompt_messages` across completed/adapter-failure/parse-failure/retry events, and the unchanged single-user audit shape for non-chatroom modes.
- [x] 2.2 Extend `ModelRequest` with an optional canonical message sequence while retaining `prompt`; chatroom populates exactly three layers and every existing non-chatroom call site remains on the absent-messages legacy path.
- [x] 2.3 Implement code-owned adapter capabilities and exact payload tests: OpenAI native developer opt-in/default merged fallback, Anthropic top-level system merge, Gemini system-instruction/default and labeled-user fallback, CLI labeled flattening, and mock observability. Capability flags must not enter `ModelConfig`, API, meeting metadata, or user settings.
- [x] 2.4 Add required `persona_summary`/backend-only `persona_prompt` definitions and validation for Host, Advisor, Critic, Strategist, and Analyst; expose only summaries. Add creation UI/API tests for mandatory Host, optional other fixed roles, exact active-roster `@all`, immutable post-create roster, old empty-roster all-five fallback versus non-empty subset Host-only append, Persona read-time fallback, and unchanged non-chatroom role definitions.
- [x] 2.5 Refactor the Slice-2 chatroom prompt builder into system/developer/user-context layers containing only normalized instruction, quote, and recent transcript in user/context. Remove the legacy chatroom case-material/body injection seam now; with no Slice-3 selected-source snapshot, no attachment/evidence/note body may be opened or appear in prompt/audit.
- [x] 2.6 Update natural-response guidance and message-only `chat-message/v1` validation for adaptive length, useful Markdown, no fixed report headings, and no post-generation truncation. Do not add `attachment_refs` or source semantic validation until 3.6.
- [x] 2.7 Verify formal modes, provider payload compatibility, prompt templates, schemas, participant behavior, and historical chatroom events remain unchanged outside the explicit Host/Persona read-time projection.

## 3. Explicit `#` attachments

- [x] 3.1 Add failing parser and composer tests for `#` source chips, uploads plus evidence, notes exclusion, multiple refs, first-selection order, deduplication, hidden stable source refs, same-label disambiguation, and independent role/source behavior.
- [x] 3.2 Add chatroom display-name role chips and source autocomplete/hints for `@指定 AI`, `#選取附件`, and default Host routing without changing the immediate-upload `+` flow.
- [x] 3.3 Implement `chatroom_sources` with the closed kind matrix and one-item mirror rule: a text upload is listed/cited only as `attachment:<file_id>` and suppresses its exactly linked mirrored evidence; independent evidence remains `evidence:<evidence_id>` even with the same label. Persist `host_acl_explicit:true` on every newly written evidence version (including mirror writes), never backfill it, allow Host legacy fallback only when absent, and otherwise enforce explicit `visible_roles` for standalone and linked sources. Unlinked legacy attachments are meeting-wide to the active roster; add legacy allow, new explicit deny, linked-mirror deny, invalid-marker, and atomic multi-target tests.
- [x] 3.4 Connect the already-enforced Slice-2 no-body boundary to explicit selected-source validation/retrieval; no `#` returns an empty source snapshot and never opens a body.
- [x] 3.5 Implement deterministic bounded retrieval and freeze one `chatroom-source-context/v1` snapshot per request/fanout containing authoritative source metadata, attachment event/file/size/SHA-256 or evidence version identity, exact retrieved segments, actual `available_segment_refs`, and omission metadata. Persist excerpt text in canonical `prompt_messages`, metadata in `selected_source_snapshot`, and reuse the same snapshot for automatic retries.
- [x] 3.6 Extend `chat-message/v1` with `attachment_refs` only now. Validate source ref, exact label, and segment refs solely against the frozen invocation snapshot; generic chat may omit. Preserve event provenance and add full/segmented/wrong-label/unknown-segment/retry/deleted-after-response/generic prompt-output-event tests.
- [ ] 3.7 Render source citation chips, connect active attachment/evidence refs to the existing reader, and show deleted/tombstoned/inactive citations as unavailable without breaking the bubble.
- [ ] 3.8 Add end-to-end tests covering no-# isolation, one selector/citation for mirrored text uploads, independent same-label evidence, legacy/unlinked attachment visibility, notes exclusion, Host fallback, multi-role/all rejection, ordered refs, bounded snapshot/retry provenance, exact citations, post-citation deletion, and existing download/delete behavior.

## 4. Shared memory and summary

- [ ] 4.1 Add failing tests and a versioned derived-memory schema/store for facts, decisions, unresolved questions, next steps, revision, update time, status/stale/error metadata, and source boundary without modifying `events.jsonl`; introduce a per-meeting `ChatroomMemoryTaskManager` separate from `MeetingJobManager`.
- [ ] 4.2 Implement a dedicated `chatroom-summary/v1` generator using the default Host model assignment without the Host chat Persona, and exclude attachment body text from the summary.
- [ ] 4.3 Add stable-round settlement hooks and threshold detection so automatic summaries reserve only after the target round is terminal, with `@all` waiting for every expected role; share an idempotent per-meeting reservation with manual regeneration.
- [ ] 4.4 Extend the context builder to use the snapshot's prior summary plus newer transcript events during generation, preserve current/quote blocks, freeze per fanout round, atomically reject stale-boundary writes, and fall back safely after failure without blocking chat.
- [ ] 4.5 Implement exact manual regeneration behavior: open chatroom/no active round/no generation and duplicate-generating calls return `202 {status,meeting_id,memory_status:"generating"}`; non-chatroom/closed/terminal/active-round calls return exact 409 envelope; memory generation never sets/consumes `MeetingJobManager` chat slots, and `chatroom_memory_updated` plus reconnect GET update the panel without feed events.
- [ ] 4.6 Add API/concurrency/websocket/projection/UI tests for summary generating + chat `202` while a chat job slot is occupied, duplicate regeneration/no duplicate task, exact rejection envelopes, `@all` terminal trigger (including completed/failed/cancelled outcomes), snapshot ordering, stale write discard/no event-log mutation, failure states, websocket update/no feed event, reconnect GET refresh, identical role context, and historical meetings without a memory file.

## 5. Integration and acceptance verification

- [ ] 5.1 Run targeted backend routing, runner, context, attachment, summary, schema, and adapter suites; fix only failures within this change.
- [ ] 5.2 Run frontend unit/type/build checks and Playwright coverage for the composer, autocomplete, citation chips, summary panel, and fanout arrival behavior.
- [ ] 5.3 Run non-chatroom regression suites and historical-event fixtures to verify formal modes, old chatroom bubbles, attachment lifecycle, and no API-key exposure.
- [ ] 5.4 Review the final diff against the OpenSpec deltas and update implementation evidence before handing the change to the independent Reviewer for Gate B.
